version: 0.0.4
name: qwen_mt_image_translation
displayName: "%display-name2%"
description: "%description2%"
scripts:
  bootstrap: |
    npm install
    poetry install --no-root
icon: "./icon.png"
dependencies:
  downloader: 0.1.5
repository: https://github.com/oomol-flows/qwen-mt-image.git
releaseNotes: "fix: hide useless block"
